import React from 'react';
// import { LoadingContext } from '../../globalState';

const Loading = () => {
  // const { loading } = useContext(LoadingContext);
  return <div className="loading bg-teal-900"/>;
};

export default Loading;
